rules_version = '2';

// Scontrino images. Same boundary as Firestore: signed in or nothing.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
